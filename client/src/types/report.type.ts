export enum Severity {
  EXTREME = "extreme",
  HIGH = "high",
  MODERATE = "moderate",
  LIGHT = "light",
}

export enum Cause {
  UNKNOWN = "unknown",
  HUMAN = "human",
  NATURAL = "natural",
}

export enum Status {
  ACTIVE = "active",
  CONTAINED = "contained",
  EXTINGUISHED = "extinguished",
}

export enum EvacuationStatus {
  NONE = "evacuation_status",
  ADVISORY = "advisory",
  MANDATORY = "mandatory",
}
